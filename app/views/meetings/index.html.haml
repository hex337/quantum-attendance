- model_class = Meeting
.page-header
  %h1
    Classes

= link_to "Create a New Class", attendance_path, { class: "btn btn-primary" }

.container
  = will_paginate @meetings, { class: "digg_pagination" }
= render 'meeting_list'
